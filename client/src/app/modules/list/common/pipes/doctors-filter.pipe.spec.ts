import { DoctorsFilterPipe } from './doctors-filter.pipe';

describe('DoctorsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DoctorsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
