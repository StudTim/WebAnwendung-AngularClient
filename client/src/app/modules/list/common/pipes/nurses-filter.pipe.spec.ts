import { NursesFilterPipe } from './nurses-filter.pipe';

describe('NursesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NursesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
